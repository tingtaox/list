import * as React from "react";
import { Switch, Link } from 'react-router-dom';

export const Header = () => {
  return (
    <ul>
      {/* <li><Link to="/list">List View</Link></li>
      <li><Link to="/detail">Detail View</Link></li> */}
    </ul>
  );
}
